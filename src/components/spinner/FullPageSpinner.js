import React from 'react';
import Loading from 'react-fullscreen-loading';

const FullPageSpinner = () => (
  <Loading loading background="#FFFFFF" loaderColor="#8da86d" />);

export default FullPageSpinner;
